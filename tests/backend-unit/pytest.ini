[pytest]
DJANGO_SETTINGS_MODULE = apps.conf.settings
# Ensure test stubs in this folder shadow heavy backend modules, then add backend path
pythonpath = . ../../backend
testpaths = tests
addopts = -q
